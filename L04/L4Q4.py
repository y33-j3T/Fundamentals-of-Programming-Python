def mean(xs):
    ''' Given parameter xs, take a sequence, 
        xs of numbers and return the arithmetic 
        mean (i.e. average value) '''
        
    return sum(xs)/len(xs)

print(mean([0, 1, 2]))